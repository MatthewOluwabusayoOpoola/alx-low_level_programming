#include<stdio.h>

/**
 * main - prints out the phrase"with proper grammer, but the outcome is a pieace of art"
 * return - 0 if success and something else in !success
 */
int main(void)
{
	printf("with proper grammer, but the outcome is a pieace of art,\n");
	return (0);
}
